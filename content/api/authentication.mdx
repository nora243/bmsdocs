---
title: Xác thực
---

# Xác thực API

## Đăng nhập

```http
POST /api/v1/auth/login
Content-Type: application/json
X-Tenant-ID: your-tenant-id

{
  "username": "admin",
  "password": "your-password"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "access_token": "eyJhbGci...",
    "expires_in": 86400
  }
}
```

## Sử dụng token

```http
GET /api/v1/purchase/orders
Authorization: Bearer eyJhbGci...
X-Tenant-ID: your-tenant-id
```

## Làm mới token

Access token hết hạn sau **24 giờ**. Dùng refresh token (cookie httpOnly) để lấy token mới:

```http
POST /api/v1/auth/refresh
X-Tenant-ID: your-tenant-id
```

## Đăng xuất

```http
POST /api/v1/auth/logout
Authorization: Bearer eyJhbGci...
X-Tenant-ID: your-tenant-id
```
